# Deployment Guide

## Production Deployments
- Frontend applications (`apps/web` & `apps/admin`) build into static SPA bundles and deploy to CDNs.
- Backend runs on secure cloud containers (e.g. AWS ECS, GCP Cloud Run).
- Database runs on managed Supabase/PostgreSQL instances.
